#pragma once

namespace Voxel {
    namespace Game {
        enum BlockType : unsigned int {
            Dirt = 0,
            Grass = 1,
            Air = 2,
            MaxValue = Air
        };
    }
}